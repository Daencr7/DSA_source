#include <bits/stdc++.h>
#define el cout<<"\n"
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n,m;cin>>n>>m;
    set <int> se;
    for (int i = 0; i < n+m; i++){
        int x; cin>>x;
        se.insert(x);
    }
    for (set<int>::reverse_iterator it = se.rbegin();it!=se.rend();++it){
        cout << *it <<" ";
    }
    return 0;
}
